import React from 'react';

const Loader = () => {
    return (
        <div className='slide-active loader'>
            <div className='loading-line'></div>
        </div>
    );
};

export default Loader;